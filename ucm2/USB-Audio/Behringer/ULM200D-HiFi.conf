Include.pcm_split.File "/common/pcm/split.conf"

SectionDevice."Mic1" {
	Comment "Mic 1"

	Macro.pcm_split.SplitPCMDevice {
		Name "ulm200d_mono_in"
		Direction Capture
		HWChannels 2
		Channels 1
		Channel0 0
		ChannelPos0 MONO
	}
}

SectionDevice."Mic2" {
	Comment "Mic 2"

	Macro.pcm_split.SplitPCMDevice {
		Name "ulm200d_mono_in"
		Direction Capture
		HWChannels 2
		Channels 1
		Channel0 1
		ChannelPos0 MONO
	}
}
